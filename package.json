{
  "name": "web-test-agent",
  "version": "0.2.0",
  "private": true,
  "description": "DeepSeek Harness browser profiles for agentic web testing: the dsh-browser bundle wired into the headless and web profiles, a demo app, and a per-profile headed-launch patcher.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "patch:headed": "node scripts/patch-dsh-browser-headed.mjs",
    "serve:demo": "python3 -m http.server 4173 --directory demo-app --bind 127.0.0.1"
  }
}
